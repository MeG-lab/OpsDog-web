name: current_time
version: 1.0.0
description: "查询系统时间"
triggers:
  - "查询时间"
  - "现在几点"
  - "time"
server_id: current_time
execution_mode: instant
task_kind: instant
entry_script:
timeout_seconds: 30
dependencies: []
